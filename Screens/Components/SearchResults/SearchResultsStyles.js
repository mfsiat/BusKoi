import { Dimensions } from "react-native";
var width = Dimensions.get("window").width;
const styles = {
    searchResultsWrapper:{
        top: 158,
        position: "absolute",
        width: width,
        height: 100,
        backgroundColor: "#fff",
        opacity: 0.9
    },
    primaryText:{
        
    }
}